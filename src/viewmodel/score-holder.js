export default class ScoreHolder {
    constructor({ 
        score,
        total,
        progress,
    }) {
        this.score = score;
        this.total = total;
        this.progress = progress;
    }
}
